services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"  # change host port if conflicts
    restart: unless-stopped

  worker:
    build: .
    command: npm run dev
    environment:
      REDIS_HOST: redis
    depends_on:
      - redis
    ports:
      - "3000:3000"
